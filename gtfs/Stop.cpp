#include <iostream>

#include <gtfs.h>

namespace gtfs {
	/**
	 * Constructor for a stop object.
	 */
	Stop::Stop (std::string& id,
		        gps::Coord& pos) : id (id), pos (pos) {};

	// GETTERS


	// SETTERS

};
